-- notes 
Setting up virtual environment in windows
    open terminal - switch to command prompt
    python -m venv venv
After setting up, activate by typing:
    venv\scripts\activate

Install required dependencies after activating by typing
    python -m pip install -r requirements.txt

